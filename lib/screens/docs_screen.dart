import 'package:flutter/material.dart';

class DocsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Documentation")),
      body: Center(child: Text("Docs Screen - Coming Soon!")),
    );
  }
}
